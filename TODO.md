Cache justwatch stuff in database.
Reload on Play to verify intent still exists.
Allow editing a show to pick the intent you want to launch

Allow picking a player.


Later
* Allow configuring different databases
